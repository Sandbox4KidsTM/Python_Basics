#covers Python Libraries

#built-in functions are automatically loaded into memory
#print(), input()

#less frequently used files have to be loaded explicitly (ie.e. import) on an as-needed basis
#random, time

import os
import random
import time

print(os.listdir()) #listing of files in current directory

print(dir(os))
time.sleep(3)
print("\n")
print("\n")
print("\n")
print(dir(random)) #this is called code introspection: understanding your code
#you don't have to know all these 
